# environment setup
ELFIE_ROOT="${HOME}/work2"
ELFIE_SOURCE="${ELFIE_ROOT}/elfie"
ELFIE_RESULTS="${ELFIE_ROOT}/results2"
ELFIE_VENV="${ELFIE_ROOT}/.venv"
ELFIE_MODULE_COLLECTION="elfie_modules"
ELFIE_MODULES=(
    "Python/3.6.1-goolfc-triton-2017a"
    )
ELFIE_OTHER_REQUIREMENTS=()
ELFIE_GIT_REPOS=(
    "${ELFIE_ROOT}/elfi2"
    "${ELFIE_ROOT}/elfie"
    "${ELFIE_ROOT}/elfirl"
    "${ELFIE_ROOT}/cogsciabc"
    )
